# ==========================================
# aitema|Hinweis – Produktions-Konfiguration
# ==========================================
#
# WICHTIG: Diese Datei ist ein Template!
# Kopieren: cp .env.production.example .env.production
# NIEMALS .env.production in Git einchecken!
#
# Sichere Passwörter generieren:
#   openssl rand -hex 32      (für Passwörter/Secrets)
#   openssl rand -base64 32   (für Base64-Secrets)
#   openssl rand -hex 64      (für lange Secrets)

# ================================
# PostgreSQL (PFLICHT)
# ================================
POSTGRES_USER=hinweis_user
POSTGRES_PASSWORD=BITTE_AENDERN_openssl_rand_hex_32
POSTGRES_DB=hinweis_platform

# ================================
# Redis (PFLICHT)
# ================================
REDIS_PASSWORD=BITTE_AENDERN_openssl_rand_hex_32

# ================================
# Anwendungs-Secrets (PFLICHT)
# ================================
# Haupt-Session-Secret (min. 64 Zeichen)
SECRET_KEY=BITTE_AENDERN_openssl_rand_hex_64

# Verschlüsselungsschlüssel für Hinweise (AES-256)
ENCRYPTION_MASTER_KEY=BITTE_AENDERN_openssl_rand_base64_32

# JWT-Signierungsschlüssel
JWT_SECRET_KEY=BITTE_AENDERN_openssl_rand_hex_32

# ================================
# CORS und URLs (PFLICHT)
# ================================
# Erlaubte Origins (kommagetrennt)
CORS_ORIGINS=https://hinweis.IHRE-DOMAIN.de

# ================================
# E-Mail (für Benachrichtigungen)
# ================================
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=hinweis@IHRE-DOMAIN.de
SMTP_PASSWORD=SMTP_PASSWORT_HIER_EINTRAGEN
SMTP_FROM=aitema Hinweis <hinweis@IHRE-DOMAIN.de>

# ================================
# Netzwerk (optional)
# ================================
HTTP_PORT=80
HTTPS_PORT=443

# ================================
# Upload-Limits
# ================================
MAX_UPLOAD_SIZE_MB=50

# ================================
# HinSchG-Compliance (PFLICHT)
# ================================
# E-Mail des Hinweisgeberbeauftragten
COMPLIANCE_EMAIL=beauftragter@IHRE-DOMAIN.de
# Frist für Eingangsbestätigung (7 Tage laut § 17 HinSchG)
CONFIRMATION_DEADLINE_DAYS=7
# Frist für Rückmeldung (3 Monate laut § 17 HinSchG)
FEEDBACK_DEADLINE_MONTHS=3

# ================================
# Backup (optional)
# ================================
BACKUP_RETENTION_DAYS=30

# ================================
# Tor-Hidden-Service (optional)
# ================================
# ENABLE_TOR=false

# ================================
# Monitoring (optional)
# ================================
# Sentry DSN für Error-Tracking
# SENTRY_DSN=https://...@sentry.io/...

# ================================
# Logging
# ================================
LOG_LEVEL=info
