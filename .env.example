# aitema|Hinweis – Umgebungsvariablen
# Kopiere diese Datei zu .env und passe die Werte an:
#   cp .env.example .env
#
# Alle mit PFLICHT markierten Variablen MÜSSEN gesetzt werden.

# ================================
# Datenbank (PFLICHT)
# ================================
DB_PASSWORD=bitte_aendern_sicheres_passwort
DATABASE_URL=postgresql://aitema:${DB_PASSWORD}@db:5432/hinweis

# ================================
# Anwendung (PFLICHT)
# ================================
# Zufälligen Schlüssel generieren: openssl rand -hex 32
SESSION_SECRET=bitte_aendern_min_32_zeichen_zufaellig

# Öffentliche URL (ohne Schrägstrich am Ende)
BASE_URL=http://localhost:3000

NODE_ENV=production
PORT=3000

# ================================
# E-Mail (optional, für Benachrichtigungen)
# ================================
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=hinweis@ihre-gemeinde.de

# ================================
# Sicherheit
# ================================
# Maximale Upload-Größe für Anhänge (in MB)
MAX_UPLOAD_SIZE_MB=10

# Sitzungs-Timeout in Minuten
SESSION_TIMEOUT_MINUTES=60

# ================================
# DSGVO / Datenschutz
# ================================
# Automatisches Löschen abgeschlossener Fälle nach X Tagen (0 = deaktiviert)
AUTO_DELETE_CLOSED_CASES_DAYS=0

# IP-Protokollierung deaktivieren (empfohlen für maximale Anonymität)
DISABLE_IP_LOGGING=true
